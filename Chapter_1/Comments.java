
 
public class Comments {
    public static void main(String[] args) {
        
        // This is a single-line comment.
        // It is used for brief explanations or notes.

        /*
         * This is a multi-line comment.
         * It has forward (/) and backward (\) slash with (*)
         * Multi-line comments, also known as block comments, begin with /* and end with */
         
         /**
           * This is a Javadoc comment.
           * Documentation comments, or Javadoc comments, are special comments used to generate\n.
           *API documentation. They start with /** and end with 
 */
                  
        System.out.println("Single-line, multi-line, and Javadoc comments are all here!");
    }
}
